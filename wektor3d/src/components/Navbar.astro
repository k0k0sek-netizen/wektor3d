---
---

<nav class="fixed top-0 left-0 w-full z-50 bg-zinc-950/80 backdrop-blur-md border-b border-white/5">
  <div class="container mx-auto px-6 h-20 flex items-center justify-between">
    
    <a href="#" class="text-xl font-bold tracking-tighter text-white z-50 relative" aria-label="Wektor 3D - Strona Główna">
      Wektor 3D
    </a>

    <div class="hidden md:flex items-center space-x-8">
          <a href="#oferta" class="text-sm font-medium text-zinc-300 hover:text-orange-500 transition-colors">Oferta</a>
          <a href="#technologia" class="text-sm font-medium text-zinc-300 hover:text-orange-500 transition-colors">Technologia</a>
          <a href="#realizacje" class="text-sm font-medium text-zinc-300 hover:text-orange-500 transition-colors">Realizacje</a>
          <a href="#kalkulator" class="text-sm font-medium text-zinc-300 hover:text-orange-500 transition-colors">Kalkulator</a>
          <a href="https://wektor-kodu.vercel.app" target="_blank" class="text-sm font-medium text-zinc-300 hover:text-teal-400 transition-colors flex items-center gap-1">
            Software
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-3 h-3">
              <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 6H5.25A2.25 2.25 0 0 0 3 8.25v10.5A2.25 2.25 0 0 0 5.25 21h10.5A2.25 2.25 0 0 0 18 18.75V10.5m-10.5 6L21 3m0 0h-5.25M21 3v5.25" />
            </svg>
          </a>
        </div>

        <div class="hidden md:flex items-center space-x-4">
          <a href="#kontakt" class="px-5 py-2.5 bg-orange-700 hover:bg-orange-600 active:bg-orange-800 active:scale-95 text-white text-sm font-bold rounded-lg transition-all shadow-lg shadow-orange-500/20">
            Kontakt
          </a>
        </div>

        <!-- Mobile Menu Button -->
        <button id="menu-btn" class="md:hidden text-zinc-300 hover:text-white focus:outline-none p-2 z-50 relative" aria-label="Otwórz menu nawigacyjne">
          <svg id="icon-open" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-8 h-8 transition-transform duration-300">
            <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
          </svg>
          <svg id="icon-close" class="w-8 h-8 absolute top-2 left-2 opacity-0 scale-50 transition-all duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
        </button>
      </div>

  <div id="mobile-menu" class="fixed inset-0 z-[100] bg-zinc-950/95 backdrop-blur-xl hidden flex flex-col items-center justify-center transition-opacity duration-300 opacity-0">
        <nav class="flex flex-col items-center space-y-8 text-center p-4">
            <a href="#oferta" class="mobile-link text-3xl font-bold text-zinc-300 hover:text-orange-500 transition-colors">Oferta</a>
            <a href="#technologia" class="mobile-link text-3xl font-bold text-zinc-300 hover:text-orange-500 transition-colors">Technologia</a>
            <a href="#realizacje" class="mobile-link text-3xl font-bold text-zinc-300 hover:text-orange-500 transition-colors">Realizacje</a>
            <a href="#kalkulator" class="mobile-link text-3xl font-bold text-zinc-300 hover:text-orange-500 transition-colors">Kalkulator</a>
            <a href="https://wektor-kodu.vercel.app" target="_blank" class="mobile-link text-2xl font-medium text-zinc-400 hover:text-teal-400 transition-colors flex items-center gap-2 justify-center">
              Software
              <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-5 h-5">
                <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 6H5.25A2.25 2.25 0 0 0 3 8.25v10.5A2.25 2.25 0 0 0 5.25 21h10.5A2.25 2.25 0 0 0 18 18.75V10.5m-10.5 6L21 3m0 0h-5.25M21 3v5.25" />
              </svg>
            </a>
            
            <div class="pt-8">
              <a href="#kontakt" class="mobile-link px-8 py-4 bg-orange-600 hover:bg-orange-500 text-white text-xl font-bold rounded-xl shadow-lg shadow-orange-500/20 transition-all">
                Kontakt
              </a>
            </div>
        </nav>
    </div>
    </nav>
  </div>
</nav>

<script>
  const btn = document.getElementById('menu-btn');
  const menu = document.getElementById('mobile-menu');
  const iconOpen = document.getElementById('icon-open');
  const iconClose = document.getElementById('icon-close');
  const mobileLinks = menu.querySelectorAll('a');
  let isOpen = false;

  function toggleMenuState() {
    isOpen = !isOpen;
    console.log('Stan menu:', isOpen ? 'Otwarte' : 'Zamknięte');
    
    if (isOpen) {
      menu.classList.remove('translate-x-full');
      document.body.style.overflow = 'hidden'; // LOCK
    } else {
      menu.classList.add('translate-x-full');
      document.body.style.overflow = ''; // UNLOCK
    }
    
    // Obsługa ikon
    if(isOpen) {
        iconOpen.classList.add('opacity-0', 'rotate-90');
        iconClose.classList.remove('opacity-0', 'scale-50');
        iconClose.classList.add('rotate-0', 'scale-100');
    } else {
        iconOpen.classList.remove('opacity-0', 'rotate-90');
        iconClose.classList.add('opacity-0', 'scale-50');
        iconClose.classList.remove('rotate-0', 'scale-100');
    }
  }

  function handleLinkClick(e) {
    if (!isOpen) return;
    e.preventDefault();
    
    const targetId = this.getAttribute('href');
    console.log('1. Kliknięto link mobilny:', targetId);

    // KROK 1: Odblokuj scroll i zamknij menu
    toggleMenuState();
    console.log('2. Menu zamknięte, scroll odblokowany');

    const targetElement = document.querySelector(targetId);
    if (targetElement) {
        // KROK 2: Poczekaj 350ms (niech menu zjedzie) i przewiń
        setTimeout(() => {
             console.log('3. Wykonuję przewinięcie do:', targetId);
             targetElement.scrollIntoView({
               behavior: 'smooth',
               block: 'start'
             });
        }, 350);
    } else if (targetId.startsWith('http')) {
        // Obsługa linków zewnętrznych (np. Wektor Kodu)
        window.open(targetId, '_blank');
    }
  }

  btn.addEventListener('click', toggleMenuState);
  mobileLinks.forEach(link => link.addEventListener('click', handleLinkClick));
</script>
